name: Create Album
method: POST
url: $base_url/albums
timeout: 0
followRedirects: true
maxRedirects: 5
headers:
  Content-Type: application/json
body: |-
  {
    "title": "my album",
    "userId": $user_id,
    "test": "$base_url",
    "bad": "$base_url"
  }
body_type: json
